# Локальные настройки Claude Code (не коммитим)
.claude/settings.local.json

# Резервная копия git-истории (создаётся filter-repo/filter-branch)
.git-backup-before-filter/

# Локальные скиллы Claude Code (не для общего репозитория)
.claude/skills/graph-pilot_TG/
